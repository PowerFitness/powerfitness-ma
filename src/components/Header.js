import React from 'react';
import styled from 'styled-components';

const StyledHeader = styled.div`
    background-color: lightblue;
    color: green;
`;

export const Header = () => <StyledHeader>PowerFitness</StyledHeader>

export default Header;
